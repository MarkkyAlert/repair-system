{
  "name": "repair-system-e2e",
  "private": true,
  "description": "Playwright E2E for the 3 golden paths (login, guest scan report, ticket lifecycle).",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1"
  }
}
